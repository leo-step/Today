const config = {
  URL: "http://127.0.0.1:8000",
  DEV: "http://127.0.0.1:8000",
  PROD: "https://today-nujm46x7ta-ue.a.run.app",
}; // TODO change to enum
// TODO define enum for localstorage keys
// TODO define enum for tracking events

export default config;
